This file contains code you can add to your .bashrc which will enable a retro style 3 line bbs prompt.
Line 1 - Shows your user login name and device + the full directory path you are in
Line 2 - If you are in a git repo, it will display the git branch for that repo
Line 3 - Cursor prompt

Example:

